export const MIN_PLAYERS = 3
export const MAX_PLAYERS = 20

export const DEFAULT_SETTINGS = {
  mode: 'classic',
  classicImposterHint: 'none',
  imposterCount: 1,
  discussionTimer: 'off',
  scoringEnabled: true,
  allowSelfVote: false,
  tieRule: 'noElimination',
  imposterGuessEnabled: false,
  imposterTeamBonus: false,
}
